# MySQL Replication with Docker and Ansible

This project demonstrates how to set up and configure MySQL Primary-Replica (Master-Slave) replication using Docker Compose and Ansible.
It provides a containerized environment with two MySQL instances and an Ansible playbook to automate the replication configuration.
